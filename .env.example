# Opcional. Sem esta chave tudo funciona: a observação da recepção é lida por palavras-chave.
# Com ela, texto que as palavras-chave não reconhecem é lido pelo Gemini (nível gratuito).
GEMINI_API_KEY=
